### Fixes

* **TabPFN `item_id` dtype**: Cast `unique_id` to string before building `TimeSeriesDataFrame`. Fixes `ValueError: all entries in index item_id must be of integer or string dtype` with `tabpfn-time-series>=1.2.0` when panel IDs come from `utilsforecast` as object dtype.

---

**Full Changelog**: https://github.com/TimeCopilot/foundationforecast/compare/v0.1.6...v0.1.7
